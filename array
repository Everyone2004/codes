#include<stdio.h>
int main ()
{ 
   int m[5],i;
for(i=0;i<5;i++)
   printf("enter the subject %d marks :",i+1);
scanf("%d",&m[i]);
for(i=0;i<5;i++)
    printf("subject=%d marks=%d",i+1,m[i]);
return 0;
}
